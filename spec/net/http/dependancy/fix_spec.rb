# frozen_string_literal: true

RSpec.describe Net::Http::Dependancy::Fix do
  it "has a version number" do
    expect(Net::Http::Dependancy::Fix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
